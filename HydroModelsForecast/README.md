# HydroModelsForecast

Forecasting layer of the **HydroModels** meta-package. **Placeholder —
no content yet.**

Planned scope when populated:

- PAR(1) inflow model fitting (Nordic convention for SDDP backward
  recursion)
- Markov price-node discretization (PRISMOD-style)
- Scenario generation utilities (historical resampling, fitted-model
  sampling)

These currently live in `HydroModelsOpt/src/uncertainty/` as stubs
tightly coupled to SDDP backward recursion. They will migrate here
once the forecasting layer takes shape.
